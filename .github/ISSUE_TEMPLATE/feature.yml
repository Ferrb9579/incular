name: Feature request
description: Propose a concrete application capability or improvement.
body:
  - type: textarea
    id: use_case
    attributes:
      label: Use case and proposed behavior
      description: Explain the application need, current workaround, affected platforms and scope.
    validations:
      required: true

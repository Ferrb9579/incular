Describe the concrete problem and resulting behavior. Note public API, platform,
dependency or architecture changes and link the relevant issue/reproduction.

Validation performed locally (commands, results, and any skipped native scenarios):

- Build/lint:
- Tests:
- Documentation/examples:

Include ownership, unsafe/FFI, cancellation, licensing or privacy considerations
when the change affects them. CI performs build checks; tests must be run locally.

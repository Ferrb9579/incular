[package]
name = "incular-scroll"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Widget-independent scrolling state, physics, and scrollbar geometry for Incular"
readme = "README.md"
publish = true

[lib]
path = "src/lib.rs"

[dependencies]
incular-core = { workspace = true }
